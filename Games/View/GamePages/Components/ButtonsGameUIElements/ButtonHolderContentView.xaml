<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Games.Behaviors"
             x:Class="Games.View.GamePages.Components.ButtonsGameUIElements.ButtonHolderContentView"
             x:Name="This">
    
    <Grid BackgroundColor="{Binding Source={x:Reference This}, Path=CurrentColor}" >
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Image Source="{Binding Source={x:Reference This}, Path=ImageSource}" Grid.Row="0" Grid.Column="0"/>
        
        <Grid.GestureRecognizers>
            <TapGestureRecognizer Tapped="Handle_Tapped" NumberOfTapsRequired="1" />
        </Grid.GestureRecognizers>
        
    </Grid>

</ContentView>
