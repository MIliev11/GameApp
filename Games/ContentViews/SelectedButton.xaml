<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:Games.Converters"
             x:Class="Games.ContentViews.SelectedButton">
    
    <ContentView.Resources>
        <ResourceDictionary>
            <converters:TypeToImageSourceConverter x:Key="sourceConverter" />
        </ResourceDictionary>
    </ContentView.Resources>
    
    <ContentView.GestureRecognizers>
        <TapGestureRecognizer Command="{Binding ButtonClickedCommand}" />
    </ContentView.GestureRecognizers>
    
    <Image Source="{Binding CurrentType, Converter={StaticResource sourceConverter}}" />
    
</ContentView>
